[phases.setup]
nixPkgs = ["python311", "libreoffice", "poppler_utils", "fontconfig", "freetype", "liberation_ttf"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 180"
